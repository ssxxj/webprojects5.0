# 第四章 XSS漏洞与防护 学生版作业说明单 v5.0

标题：XSS漏洞与防护 作业
适用章节：第四章 XSS漏洞与防护
建议时长：4-5 小时
提交格式：PDF
文件命名：`<学号><姓名>XSS漏洞与防护.pdf`

## 一、这次作业要完成什么

1. 本次作业对应的能力目标：
   - 解释浏览器不仅显示内容，还会在不同上下文中解析和执行内容
   - 区分 Reflected XSS、Stored XSS、DOM XSS 的形成路径差异
   - 在 DVWA 中观察和记录基础 XSS 现象，并说明输入、输出与执行的关系
   - 比较 Low 与 Medium 在防护思路上的差异，而不是只记绕过样例
   - 用“输入—输出—解析—执行”的链条解释风险与防护重点
2. 完成后你应能做到什么：
   - 能按本章主线组织证据，而不是只罗列任务。
   - 能把截图、字段、现象和专业判断对应起来。
   - 能说明自己是否使用 AI，以及是否审核、删改、核实过 AI 输出。

## 二、授权边界与红线

1. 允许：DVWA、本机浏览器、localhost、127.0.0.1，或教师明确授权的实验环境。
2. 禁止：对未授权真实目标进行扫描、抓包、上传、注入、XSS、命令执行、认证绕过或其他测试。
3. 脱敏要求：截图中的 Cookie、SessionID、Token、账号、路径、主机信息等敏感字段必须脱敏。
4. 红线说明：
   - 未脱敏：截图、账号、Cookie 或其他敏感信息未按要求脱敏
   - 超出授权边界：对未授权真实目标进行 XSS 观察、验证或其他测试
   - 证据伪造或不可追溯：现象截图、说明与实验过程明显不一致，无法支撑结论
   - 高度雷同或抄袭代做：与他人作业高度雷同，或存在抄袭、代做情形
5. 雷同/抄袭说明：
   - 与同学高度雷同、抄袭、代做，按课程诚信红线处理。

## 三、你需要提交什么

1. 必交截图：
   - 浏览器解析与输出上下文基础说明 对应截图或过程证据
   - Reflected XSS Low 现象记录 对应截图或过程证据
   - Stored XSS Low 现象记录 对应截图或过程证据
   - DOM XSS 与类型差异分析 对应截图或过程证据
   - Low/Medium 对照与 XSS 防护清单 对应截图或过程证据
2. 必交文字说明：
   - 浏览器解析与输出上下文基础说明 的文字说明：浏览器会解析而不只是显示内容；至少2种输出位置或上下文；为什么同样输入会有不同风险
   - Reflected XSS Low 现象记录 的文字说明：实验页面或目标；输入点与即时回显；执行或页面变化现象
   - Stored XSS Low 现象记录 的文字说明：实验页面或目标；输入被保存的证据；后续访问中的持续影响
   - DOM XSS 与类型差异分析 的文字说明：DOM XSS 的基本现象或路径；为什么更接近前端环境变化；至少1个与 Reflected/Stored 的差异
   - Low/Medium 对照与 XSS 防护清单 的文字说明：Low 与 Medium 至少1项差异；说明有限过滤的局限；输出编码或上下文安全控制
3. 必交 XSS类型差异与风险链关系图/表：
   - 不能只写工具或概念列表，必须体现关系、流程、因果和下一步。
   - 合格写法至少要说明“能说明什么 / 不能替代什么 / 下一步为什么是这个”。
4. 必交 AI输出审核与人工复核记录（必填）
5. 必交学生自评表

## 四、你要按四步完成

### Step 1 观察
- 你需要看到什么：与本章主线直接相关的现象、页面变化、参数、响应、路径、结构或流程。
- 你需要记录什么字段：按每个任务最小字段逐项记录，不要只贴图不说明。

### Step 2 判断
- 你需要做出什么专业判断：根据本章主线，判断风险发生在哪一层、为什么成立、哪些结论还不能直接下。
- 你为什么这样判断：必须回到截图、字段、现象和关系链，而不是直接下抽象结论。

### Step 3 证据
- 你要用什么支持判断：截图、参数、响应、状态码、页面变化、结构图、比较表、流程图等。
- 你必须标出哪些关键字段：
  - 1. 浏览器解析与输出上下文基础说明：浏览器会解析而不只是显示内容；至少2种输出位置或上下文；为什么同样输入会有不同风险；至少1个安全关注点
  - 2. Reflected XSS Low 现象记录：实验页面或目标；输入点与即时回显；执行或页面变化现象；为什么属于即时回显路径
  - 3. Stored XSS Low 现象记录：实验页面或目标；输入被保存的证据；后续访问中的持续影响；为什么与反射型不同
  - 4. DOM XSS 与类型差异分析：DOM XSS 的基本现象或路径；为什么更接近前端环境变化；至少1个与 Reflected/Stored 的差异；输入—输出—解析链条解释
  - 5. Low/Medium 对照与 XSS 防护清单：Low 与 Medium 至少1项差异；说明有限过滤的局限；输出编码或上下文安全控制；至少3条防护建议

### Step 4 推理
- 如果条件改变，会发生什么：说明如果某个控制点加入或移除，结果为什么会变化。
- 下一步应该做什么，为什么：写出最合理的后续验证、比较或防护动作。

### 证据 -> 结论映射
- 我看到了什么证据：
- 这个证据最多能支持到什么结论：
- 哪些话我不能直接下结论：

## 五、任务与分值

### 任务1：浏览器解析与输出上下文基础说明
- 目标：解释浏览器不仅显示内容，还会在不同上下文中解析和执行内容
- 最小字段：浏览器会解析而不只是显示内容；至少2种输出位置或上下文；为什么同样输入会有不同风险；至少1个安全关注点
- 分值：3.0 分

### 任务2：Reflected XSS Low 现象记录
- 目标：区分 Reflected XSS、Stored XSS、DOM XSS 的形成路径差异；在 DVWA 中观察和记录基础 XSS 现象，并说明输入、输出与执行的关系
- 最小字段：实验页面或目标；输入点与即时回显；执行或页面变化现象；为什么属于即时回显路径
- 分值：3.0 分

### 任务3：Stored XSS Low 现象记录
- 目标：在 DVWA 中观察和记录基础 XSS 现象，并说明输入、输出与执行的关系；区分 Reflected XSS、Stored XSS、DOM XSS 的形成路径差异
- 最小字段：实验页面或目标；输入被保存的证据；后续访问中的持续影响；为什么与反射型不同
- 分值：3.0 分

### 任务4：DOM XSS 与类型差异分析
- 目标：比较 Low 与 Medium 在防护思路上的差异，而不是只记绕过样例；区分 Reflected XSS、Stored XSS、DOM XSS 的形成路径差异
- 最小字段：DOM XSS 的基本现象或路径；为什么更接近前端环境变化；至少1个与 Reflected/Stored 的差异；输入—输出—解析链条解释
- 分值：3.0 分

### 任务5：Low/Medium 对照与 XSS 防护清单
- 目标：用“输入—输出—解析—执行”的链条解释风险与防护重点；比较 Low 与 Medium 在防护思路上的差异，而不是只记绕过样例
- 最小字段：Low 与 Medium 至少1项差异；说明有限过滤的局限；输出编码或上下文安全控制；至少3条防护建议
- 分值：3.0 分

### XSS类型差异与风险链关系图/表
- 要求：必须体现关系、流程、因果、控制点与下一步，不能退化成孤立概念或工具清单。
- 分值：3.0 分
- 三档说明：
  1. 未提交：0 分
  2. 已提交但关系不足：按部分完成扣分
  3. 关系表达完整：满分

### 学生自评表
- 分值：2.0 分

## 六、AI输出审核与人工复核记录（必填）

```text
1. 本作业是否使用 AI：未使用 / 局部使用 / 深度使用
2. 若使用 AI，AI 主要帮助了哪些环节：
3. 我保留了 AI 输出中的哪 2 处内容，为什么保留：
4. 我删改了 AI 输出中的哪 2 处内容，为什么删改：
   若未使用 AI，改填：本作业未使用 AI；我主动删掉或放弃的 2 处思路是什么，为什么放弃。
5. 本作业中我自行核实过的 3 个专业判断：
6. 我认为最容易误判的一处边界问题：
```

提醒：

1. 不能只写“已审核”“已检查”，必须写清保留、删改和核实内容。
2. 若未使用 AI，也要写清自己主动放弃了哪些思路，以及为什么放弃。

## 七、提交前核对表

- [ ] 我已完成所有任务
- [ ] 我已给出必要截图和文字说明
- [ ] 我已标注关键字段
- [ ] 我已完成 XSS类型差异与风险链关系图/表
- [ ] 我已填写 `AI输出审核与人工复核记录（必填）`
- [ ] 我已明确自己是否使用 AI，以及是否审核、删改、核实过 AI 输出
- [ ] 我已完成脱敏
- [ ] 我没有超出授权边界
- [ ] 我没有照搬他人作业或与同学高度雷同

## 八、你会怎样被扣分

1. 缺少一个任务：扣该任务分值。
2. 任务已做但内容不全：在该任务分值内按比例扣分。
3. XSS类型差异与风险链关系图/表 已交但主要停留在工具解释、没有体现关系：按部分完成扣分。
4. 自评表、关键字段、局部规范性问题：小幅扣分。

## 九、常见误区提醒

1. 关系图/关系表不是工具清单，必须体现关系链。
2. 截图不是越多越好，关键字段必须能看清。
3. 线索不等于证据，端口不等于漏洞，第三方索引不等于实时探测。
4. AI 可以用，但不审核就等于没有完成本项要求。

## 十、哪些情况会进入红线处理

1. `未脱敏`：截图、账号、Cookie 或其他敏感信息未按要求脱敏，处理方式：`lt_60`。
2. `超出授权边界`：对未授权真实目标进行 XSS 观察、验证或其他测试，处理方式：`lt_60`。
3. `证据伪造或不可追溯`：现象截图、说明与实验过程明显不一致，无法支撑结论，处理方式：`lt_60`。
4. `高度雷同或抄袭代做`：与他人作业高度雷同，或存在抄袭、代做情形，处理方式：`lt_60`。

## 十一、学生自评表

```text
1. 任务完成情况
2. 机制与关系链理解情况
3. 证据质量情况
4. 边界与风险判断情况
5. AI输出审核与人工复核记录情况
6. 本次最需要改进的一项
```
