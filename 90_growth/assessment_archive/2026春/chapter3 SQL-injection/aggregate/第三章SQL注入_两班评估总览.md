[meta]
type: EvaluationOverview
status: Final
version: v5.0
keywords: 第三章,SQL注入,两班对比,最终评估
owner: Codex
updated: 2026-05-02
[/meta]

# 第三章 SQL注入漏洞与防护 两班评估总览

## 一、最终结论

1. 本轮结果已经切换为“新引擎直出”，不再依赖人工校正成绩表。
2. `收口项2：学生自评表` 的问答式、收口项式、章节式写法已纳入识别；`AI 输出审核记录` 的空格变体也已纳入识别。
3. 第三章原先最主要的系统误差已经清理：授权边界误判清零，残余的 `学生自评表` / `AI审核记录` 缺失均已压缩到人工复核确认的真实缺失范围。
4. 按最终结果看，`24信安02` 仍整体高于 `24信安01`，均分分别为 `87.88` 与 `85.58`。

## 二、班级数据

1. `24信安01`：提交 `26` 份，均分 `85.58`，优秀 `8`，良好 `13`，中等 `4`，及格 `1`，待补救 `0`。
2. `24信安02`：提交 `25` 份，均分 `87.88`，优秀 `14`，良好 `9`，中等 `1`，及格 `0`，待补救 `1`。

## 三、复核结论

1. `学生自评表` 真实缺失：`24信安01` 还剩 `2` 份，`24信安02` 还剩 `3` 份。此前被漏识别的 `任务6：自评`、`收口项2 学生自评`、`六、学生自评` 已修正。
2. `AI输出审核与人工复核记录` 真实缺失：`24信安01` `2` 份，`24信安02` `4` 份；另有“已出现但不完整”分别为 `10` 份和 `8` 份。
3. `授权边界` 红线误判已清零：两班当前均为 `0` 份。
4. `关系图` 仍是主要失分点：`24信安01` 完整 `6` 份、部分完成 `20` 份；`24信安02` 完整 `16` 份、部分完成 `9` 份。

## 四、说明

1. 最终 `xlsx` 已按学号顺序重建为“新引擎分数/等级/优点/建议”格式。
2. 最终 `md` 教学反馈报告与本总览口径一致，全部来自本次复核后的 debug 数据。
